# Codex Install

From the parent workspace:

```powershell
Set-Content -Path .\AGENTS.md -Value "Read .helicopter-harness/adapters/codex/AGENTS.md first."
```

Do not copy Helicopter-Harness into `%USERPROFILE%\.codex\skills` by default. This harness is intended to live with the parent workspace.

